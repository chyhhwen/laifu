# Force Gradle (and Kotlin DSL) to run on a supported JDK.
# The project itself uses toolchain(21), but Gradle was launched with JDK 25 which breaks Kotlin's JavaVersion parsing.
org.gradle.java.home=/Users/a123/Library/Java/JavaVirtualMachines/ms-21.0.10/Contents/Home
